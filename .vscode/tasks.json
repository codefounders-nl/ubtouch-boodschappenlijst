{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Clickable desktop",
            "type": "shell",
            "command": "PATH=$PATH:/home/leandro/.local/bin clickable --skip-review --dirty desktop",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}